<!DOCTYPE html>
<html>
<head>
    <title>Тест - {{ test_name }}</title>
    <link rel="stylesheet" href="{{ url_for('static', filename='style.css') }}">
</head>
<body>
    <h1>Тест по {{ test_name }}</h1>
    <p>Здравствуйте, {{ fio }}!</p>
    <p>Выберите один или несколько вариантов ответа. В каждом вопросе есть как минимум один верный вариант ответа.</p>

    <h2>{{ question }}</h2>  <!--  Отображаем текст вопроса -->
    <form method="POST">
        {% for option in options %}
            <input type="checkbox" id="answer_{{ loop.index0 }}" name="answer" value="{{ option }}" name="answer">
            <label for="answer_{{ loop.index0 }}">{{ option }}</label><br>
        {% endfor %}
        <button type="submit">Ответить</button>
    </form>
</body>
</html>
